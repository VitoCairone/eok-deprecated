// Rendering changes with Rails Unobtrusive JS is pretty cool,
// but this super simple method lacks any transition effect and feels a bit stiff.
// Look to add some nice transition animations in the future.

// console.log("Selected: '<%= @answer.text %>'")
$("#question-<%= @question.id %>-wrapper").html("<%= j(render 'respond', question: @question) %>");
// console.log("Rerender done")